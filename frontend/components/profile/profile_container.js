import { connect } from 'react-redux';
import Profile from './profile';

const msp = state => {
  return (

  );
}

const mdp = dispatch => {
  return (
    
  );
}

export default connect(msp, mdp)(Profile);